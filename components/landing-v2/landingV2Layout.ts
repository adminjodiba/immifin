/**
 * LOCKED /landing-v2 layout tokens — copy of the approved V7 content grid.
 * Do not change. Design System experiments belong in landing-v3/landingV3Layout.ts.
 */
export const landingV2ContentGridClass =
  "landing-v3-content-grid mx-auto w-full px-4 sm:px-6 lg:px-8";

/** Chrome + Hero text content wrapper — same grid as body. */
export const landingV2ContainerClass = landingV2ContentGridClass;

/** Final CTA / explicit body container — same grid as Hero text. */
export const landingV2BodyContainerClass = landingV2ContentGridClass;

/**
 * Wrapper that widens nested `.container-main` used by WorkspaceSection /
 * V2 body components.
 */
export const landingV2BodyShellClass = "landing-v3-body landing-v6-body";
